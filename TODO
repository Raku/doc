- In 101 tutorial, `(Basics)` is out of place and should not be there, but removing it we will break anchors
- In 5to6-perlfunc, the `- perlfunc` thing is ad-hoc and should not be there, but removing it will break anchors
* Operators page has broken links caused by improper formatting, should be completely re-taken.
  * It has an attempt to index concatenated `!==` operator, but it is highly insufficient and does not solve the main issue
* Pragmas page is COMPLETELY broken, every item there has wrong format, everything breaks
* Indexig of traits is just bad and inconsistent, needs a complete re-take
