Note: you can now trigger a rebuild on the build machine directly:

    sudo su -l doc.perl6.org
    touch ~/force-update

But, if you want to, you can still trigger a rebuild by making commits
with changes in this file.

##################
