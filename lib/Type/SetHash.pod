=begin pod

=TITLE class SetHash

    class SetHash does Setty { }

A mutable collection of unique values. These values can be accessed via the
C<{ }> postcircumfix, and always have a value of either C<True> or C<False>. For an
immutable collection, see L<Set>.

=end pod
