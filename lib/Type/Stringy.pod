=begin pod

=TITLE role Stringy

=SUBTITLE Common role for strings and types that can act as strings

    role Stringy { ... }

Common role for string types (such as Str) and types that can act as
strings (such as Cat).

=end pod
