=begin pod

=TITLE class BagHash

    class BagHash does Baggy { }

A mutable collection of weighted values. These weights are always
integers, and can be accessed with the C<{ }> postcircumfix. For an
immutable collection of integrally weighted values, see L<Bag>.

=end pod
