os:
    - linux

cache:
    directories:
        - precompiled
        - html
        
build:
  pre_ci:
    - docker pull jjmerelo/perl6-doccer
    - docker volume inspect $SHIPPABLE_CONTAINER_NAME

  ci:
    - docker run -t -v $SHIPPABLE_BUILD_DIR:/test jjmerelo/perl6-doccer


