os:
    - linux

cache:
    directories:
        - precompiled
        - html
        
build:
  pre_ci_boot:
    options: -v /root:/root
      
  pre_ci:
    - docker pull jjmerelo/perl6-doccer

  ci:
    - docker run -t -v /root/test:/test jjmerelo/perl6-doccer


