os:
    - linux

cache:
    directories:
        - precompiled
        - html
        
build:
  pre_ci:
    - docker pull jjmerelo/doccer

  ci:
    - docker run -t -v $TRAVIS_BUILD_DIR:/test jjmerelo/perl6-doccer


